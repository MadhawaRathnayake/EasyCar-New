{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"node app.js\" \"node socket/app.js\" \"npm --prefix client run dev\"",
    "start-socket": "node socket/app.js",
    "build": "npm install && npm --prefix client install && npm --prefix socket install && npm --prefix client run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "path": "^0.12.7",
    "prisma": "^5.14.0"
  }
  ,
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
